

export default function layout({ children }: { children: React.ReactNode }) {
  return (
    <>
      <head>
        <title>Chatter|Confirm Email</title>
        <meta name='description' content="" />
      </head>
      {children}
    </>
  )
}